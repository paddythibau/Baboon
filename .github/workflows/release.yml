name: Release

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  # audiopus_sys vendors libopus and compiles it from source (statically linked
  # into the binary — no runtime libopus needed). Its bundled CMakeLists targets
  # an old CMake; allow the runners' modern CMake (4.x) to configure it.
  CMAKE_POLICY_VERSION_MINIMUM: "3.5"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: ${{ github.ref == 'refs/heads/main' }}

jobs:
  build:
    name: ${{ matrix.artifact }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact: baboon-linux-x86_64
            binary: Baboon
            archive_ext: tar.gz
          - os: macos-latest
            artifact: baboon-macos-aarch64
            binary: Baboon
            archive_ext: tar.gz
          - os: windows-latest
            artifact: baboon-windows-x86_64
            binary: Baboon.exe
            archive_ext: zip

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Install Linux build deps
        if: runner.os == 'Linux'
        run: |
          # Chrome is preinstalled on the runner and Baboon does not use its
          # apt repository. Do not let desynchronized third-party metadata
          # prevent installation of the Ubuntu libraries below.
          for source_file in /etc/apt/sources.list.d/*; do
            if grep -q 'dl.google.com/linux/chrome' "$source_file"; then
              sudo mv "$source_file" "$source_file.disabled"
            fi
          done
          sudo apt-get update
          # libasound2-dev: ALSA headers for rodio's Linux backend (cpal/alsa-sys).
          # libxcb1-dev: links the `xcb` crate, which window_state uses to read
          # X11 work areas. The runners ship libxcb1 but not its .so symlink.
          # libopus is NOT needed — it's vendored + statically compiled.
          sudo apt-get install -y libxkbcommon-dev libasound2-dev libxcb1-dev

      - name: Build release
        run: cargo build --release

      - name: Stage artifact
        shell: bash
        run: |
          mkdir -p staging/${{ matrix.artifact }}
          cp target/release/${{ matrix.binary }} staging/${{ matrix.artifact }}/
          cp -r definitions staging/${{ matrix.artifact }}/
          cp -r docs staging/${{ matrix.artifact }}/
          cp README.md staging/${{ matrix.artifact }}/

      - name: Archive (tar.gz)
        if: matrix.archive_ext == 'tar.gz'
        shell: bash
        run: |
          cd staging
          tar -czf ../${{ matrix.artifact }}.tar.gz ${{ matrix.artifact }}

      - name: Archive (zip)
        if: matrix.archive_ext == 'zip'
        shell: pwsh
        run: |
          Set-Location staging
          Compress-Archive -Path ${{ matrix.artifact }} -DestinationPath ../${{ matrix.artifact }}.zip

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}.${{ matrix.archive_ext }}
          if-no-files-found: error

  publish:
    name: Publish
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      # Attaches the built archives and nothing else. `generate_release_notes`
      # is deliberately absent: a tagged release is usually published from a
      # draft whose notes were written by hand, and generating them here would
      # replace those with a commit list at the moment the tag is pushed.
      # Passing no body leaves whatever the release already has.
      - name: Tagged release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          fail_on_unmatched_files: true

      - name: Dev rolling release
        if: github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete dev --yes --cleanup-tag 2>/dev/null || true
          gh release create dev artifacts/* \
            --target "$GITHUB_SHA" \
            --prerelease \
            --title "Development Build" \
            --notes "Rolling build from \`main\`. Updated on every push.

          Commit: [\`${GITHUB_SHA::7}\`](https://github.com/${GITHUB_REPOSITORY}/commit/${GITHUB_SHA})
          Built: $(date -u +'%Y-%m-%d %H:%M UTC')

          > These builds are unsigned. macOS users may need \`xattr -d com.apple.quarantine\` on the binary; Windows users will see a SmartScreen warning."
